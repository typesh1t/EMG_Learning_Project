# 样本数据说明

## 📦 关于本目录

本目录用于存放**样本EMG数据**，供学习和测试使用。

## 🎯 数据来源

样本数据可以来自：

### 1. 生成的模拟数据 ⭐ 推荐初学者
使用 `tools/generate_sample_data.py` 脚本生成模拟的EMG信号。

**生成方法**:
```bash
# 进入tools目录
cd ../tools

# 生成样本数据
python generate_sample_data.py \
    --output ../data/sample/ \
    --subjects 5 \
    --trials 10 \
    --gestures rest fist open \
    --channels 4 \
    --fs 1000 \
    --duration 5.0
```

**优点**:
- ✅ 无需硬件设备
- ✅ 立即可用
- ✅ 参数可调
- ✅ 适合学习信号处理和机器学习

**缺点**:
- ❌ 不是真实数据
- ❌ 信号特征简化

---

### 2. 下载公开数据集

#### UCI EMG Gestures Dataset（强烈推荐）

**下载方法**:
```bash
# 方法1：Python下载（推荐）
python ../tools/download_uci_dataset.py --output ./uci_emg/

# 方法2：手动下载
# 访问: https://archive.ics.uci.edu/ml/datasets/EMG+data+for+gestures
# 下载后解压到: ./uci_emg/
```

详细说明见：[docs/数据集资源指南.md](../../docs/数据集资源指南.md)

---

### 3. 自己采集数据

使用EMG设备采集真实数据，参考：
- [docs/手部数据采集指南.md](../../docs/手部数据采集指南.md)

采集完成后：
- 原始数据放在 `../raw/`
- 处理后数据放在 `../processed/`
- 样本数据（用于演示）可以放在这里

---

## 📂 推荐的目录结构

```
sample/
│
├── README.md (本文件)
│
├── uci_emg/              # UCI数据集
│   ├── subject1/
│   ├── subject2/
│   └── ...
│
├── generated/            # 生成的模拟数据
│   ├── subject_01/
│   │   ├── rest/
│   │   ├── fist/
│   │   └── open/
│   └── subject_02/
│       └── ...
│
└── quick_demo/          # 用于快速演示的小样本
    ├── sample_rest.csv
    ├── sample_fist.csv
    └── sample_open.csv
```

---

## 🚀 快速开始

### 步骤1：生成样本数据

```bash
cd ../tools
python generate_sample_data.py --output ../data/sample/generated/ --subjects 2 --trials 5
```

### 步骤2：加载数据

```python
# 进入Python
import sys
sys.path.append('../tools')
from data_loader import EMGDataLoader

# 创建加载器
loader = EMGDataLoader(dataset_type='sample')

# 加载数据
emg, label, time = loader.load('../data/sample/generated/subject_01/fist/trial_001.csv')

print(f"EMG形状: {emg.shape}")
print(f"标签: {label[0]}")
```

### 步骤3：可视化

```python
import matplotlib.pyplot as plt

plt.figure(figsize=(12, 6))
for i in range(4):
    plt.subplot(4, 1, i+1)
    plt.plot(time, emg[:, i])
    plt.ylabel(f'Ch{i}')
    if i < 3:
        plt.xticks([])
plt.xlabel('Time (s)')
plt.tight_layout()
plt.show()
```

---

## 📊 数据格式说明

所有样本数据统一使用CSV格式：

```csv
time,channel_0,channel_1,channel_2,channel_3,label
0.000,0.023,-0.015,0.008,0.012,fist
0.001,0.025,-0.018,0.010,0.015,fist
...
```

**列说明**:
- `time`: 时间戳（秒）
- `channel_X`: 第X通道的EMG信号（mV）
- `label`: 手势标签（rest/fist/open等）

---

## 💡 使用建议

### 学习阶段建议

| 学习阶段 | 推荐数据 | 原因 |
|---------|---------|------|
| 第3周：信号特征 | 生成的模拟数据 | 信号干净，特征明显 |
| 第5周：数据处理 | 生成的模拟数据 | 快速验证代码 |
| 第6周：信号预处理 | 生成的数据（带噪声） | 测试滤波器效果 |
| 第7周：特征提取 | UCI或生成数据 | 数据量适中 |
| 第8周：模式识别 | UCI数据集 | 真实数据，更有挑战 |
| 第9-10周：最终项目 | 自己采集的数据 | 完整体验 |

---

## 🔗 相关资源

- **数据集资源**: [docs/数据集资源指南.md](../../docs/数据集资源指南.md)
- **采集指南**: [docs/手部数据采集指南.md](../../docs/手部数据采集指南.md)
- **生成脚本**: [tools/generate_sample_data.py](../../tools/generate_sample_data.py)
- **加载工具**: [tools/data_loader.py](../../tools/data_loader.py)

---

## ❓ 常见问题

### Q: 我应该用模拟数据还是真实数据？
A: 两者结合：
- 前期（3-6周）用模拟数据快速学习
- 后期（7-10周）用真实数据训练实用模型

### Q: 模拟数据和真实数据的主要区别？
A:
- **模拟数据**: 更规律，噪声可控，特征明显
- **真实数据**: 更复杂，个体差异大，挑战性强

### Q: 生成的数据可以用于最终项目吗？
A: 可以作为演示和测试，但建议至少包含部分真实数据（UCI数据集或自己采集）。

---

**准备好开始了吗？先生成一些样本数据试试吧！** 🚀
